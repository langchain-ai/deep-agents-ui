# ============================================================
# Seenos UI 环境变量配置
# ============================================================
# 复制此文件为 .env.local (本地开发) 或 .env.production (生产环境)
# 并填入实际值
# ============================================================

# ============ API 配置 ============

# 后端 API 基础 URL
# 本地开发: http://localhost:8000
# 测试环境: https://api-test.seenos.ai
# 生产环境: https://api.seenos.ai
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL (可选，不填则自动从 API_URL 推导)
# 本地开发: ws://localhost:8000/api/chat/stream
# 测试环境: wss://api-test.seenos.ai/api/chat/stream
# 生产环境: wss://api.seenos.ai/api/chat/stream
# NEXT_PUBLIC_WS_URL=

# SSE URL (备选方案，WebSocket 不可用时使用)
# 本地开发: http://localhost:8000/api/chat/stream
# 测试环境: https://api-test.seenos.ai/api/chat/stream
# 生产环境: https://api.seenos.ai/api/chat/stream
# NEXT_PUBLIC_SSE_URL=

# ============ 功能开关 ============

# 默认传输方式: websocket 或 sse
NEXT_PUBLIC_DEFAULT_TRANSPORT=websocket

# 是否启用调试模式 (显示额外的调试信息)
NEXT_PUBLIC_DEBUG=false

# ============ LangGraph 配置 (可选) ============
# 如果使用 LangGraph 部署模式，配置以下变量

# LangSmith API Key (格式: lsv2_pt_...)
# NEXT_PUBLIC_LANGSMITH_API_KEY=

# ============ 分析与监控 (可选) ============

# Google Analytics ID
# NEXT_PUBLIC_GA_ID=

# Sentry DSN
# NEXT_PUBLIC_SENTRY_DSN=
